# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Pydantic / settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator>=2.0.0

# Auth
bcrypt==4.2.1
python-multipart==0.0.17

# AI — Anthropic stays as a fallback provider for dev / existing test mocks.
# OpenAI SDK is the path forward: per the 05/02 product call, Schiller CPA
# runs Gemma on RunPod via vLLM, which exposes an OpenAI-compatible API.
# Same SDK works against OpenAI proper, RunPod, vLLM, llama.cpp's server.
anthropic==0.40.0
openai>=1.50.0,<2.0.0

# HTTP client (MS Graph, webhooks)
httpx==0.28.1

# OS-native cert store for HTTPS — required when TLS-intercepting middleware
# (corporate proxies, AV like Norton) re-signs certs with a private root that
# certifi's bundle doesn't know about. truststore.inject_into_ssl() is called
# at app startup so httpx/openai use the OS trust store instead of certifi.
truststore>=0.10,<1.0

# Email / IMAP
# imaplib and smtplib are part of stdlib — no extra package needed

# Environment
python-dotenv==1.0.1

# Utilities
python-dateutil==2.9.0
