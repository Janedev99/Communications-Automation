[pytest]
testpaths = tests
asyncio_mode = auto
# Suppress the default warning about using the insecure dev secret key
filterwarnings =
    ignore::UserWarning:app.config
    ignore::DeprecationWarning
